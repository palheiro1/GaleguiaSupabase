// Galeguia Supabase Configuration
// IMPORTANT: Replace these values with your actual Supabase project credentials
// You can find these in your Supabase project dashboard under Project Settings > API

export const SUPABASE_URL = 'https://your-project-id.supabase.co'
export const SUPABASE_ANON_KEY = 'your-supabase-anon-key'

// For React/Next.js/Expo projects, you'll typically use these values as follows:
/*
import { createClient } from '@supabase/supabase-js'
import { SUPABASE_URL, SUPABASE_ANON_KEY } from './supabase-config'

// Initialize the Supabase client
const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)

// Export the client for use in your application
export default supabase
*/
